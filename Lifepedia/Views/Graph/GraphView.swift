import SwiftUI

struct GraphView: View {
    var body: some View {
        NavigationStack {
            Text("知识图谱")
                .foregroundColor(.wikiGray)
                .navigationTitle("图谱")
        }
    }
}
