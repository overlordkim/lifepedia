import SwiftUI

struct ComposeView: View {
    var body: some View {
        NavigationStack {
            Text("编纂 — AI 对话创作")
                .foregroundColor(.wikiGray)
                .navigationTitle("编纂")
        }
    }
}
