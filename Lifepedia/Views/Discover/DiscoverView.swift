import SwiftUI

struct DiscoverView: View {
    var body: some View {
        NavigationStack {
            Text("发现页 — 双列瀑布流")
                .foregroundColor(.wikiGray)
                .navigationTitle("发现")
        }
    }
}
